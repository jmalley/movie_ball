<h2>Welcome!</h2>

<%# if current_user.league %>
<ul>
  <% @leagues.each do |league| %>
    <li><%= link_to league.name, league %> | <%# link_to "Join", member_join(current_user) %></li>
  <% end %>
</ul>
<%# end %>